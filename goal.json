{
  "scalar": null,
  "gates": [
    {
      "name": "tdd_red_evidence",
      "cmd": "test -n \"$TDD_RED_EVIDENCE\" || npm test -- src/__tests__/kernel/engine.test.ts",
      "must": true
    },
    {
      "name": "build",
      "cmd": "npm run build",
      "must": true
    },
    {
      "name": "lint",
      "cmd": "npm run lint",
      "must": true
    },
    {
      "name": "format",
      "cmd": "npm run format:check || npm run format -- --check",
      "must": true
    },
    {
      "name": "kernel_engine_tests",
      "cmd": "npm test -- src/__tests__/kernel/engine.test.ts",
      "must": true
    },
    {
      "name": "full_suite",
      "cmd": "npm test",
      "must": true
    }
  ],
  "termination": {
    "max_iters": 15
  }
}